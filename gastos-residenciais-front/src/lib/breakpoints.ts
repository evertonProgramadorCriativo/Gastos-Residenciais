// Fonte única de verdade para os breakpoints usados em JS (useMediaQuery).

export const BREAKPOINT_TABLET = 768;
export const BREAKPOINT_MOBILE = 480;

export const BREAKPOINT_TABELA = 640;

export const QUERY_TABLET_DOWN = `(max-width: ${BREAKPOINT_TABLET}px)`;
export const QUERY_MOBILE_DOWN = `(max-width: ${BREAKPOINT_MOBILE}px)`;
export const QUERY_TABELA_DOWN = `(max-width: ${BREAKPOINT_TABELA}px)`;
